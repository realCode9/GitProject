#   add local changes first -> git add .
#   then stash changes so that they will disappear from the terminal -> git stash
#   then see what all branches are there at remote --> git fetch --all
#   take pull from the desired branch --> git pull origin branch_name
#   apply git stash so that stashed changes will come back --> git stash apply
#   now see are there any conflicts are and resolve them
#   see the status now after resolving conflicts -> git status
